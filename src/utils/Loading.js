import React from 'react';
import LoadingComponent from 'react-loading-components';

const  Loading = () => (
    <LoadingComponent type='spinning_circles' width={100} height={100} fill='#275073' />
);

export default Loading